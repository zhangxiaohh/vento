import { A as AstroError, M as MissingLocale } from './astro/server_DNwJ7Yw1.mjs';

const baseStrings$2 = {
  "site.title": "vento",
  "site.desc": "这是我的个人博客，用来分享我的点滴成长。"
};
const ENLocale = {
  ...baseStrings$2,
  "hero.title": "Hello, I'm vento",
  "hero.typingTexts": [
    "这里是我的博客，一个由代码、文字和想法构成的数字空间。",
    "我写技术，也写生活；聊效率，也聊美学。",
    "我不追求日更，只在乎每一次落笔是否足够真诚。",
    "代码是逻辑的表达，文字是思想的载体。",
    "在这里，记录比完美更重要。"
  ],
  "hero.beforeReadme": "阅读下面的信息，你就能找到我：",
  "hero.readme": "",
  "hero.afterReadme": "",
  copy: "copy",
  copied: "copied",
  by: "by",
  about: "About",
  socialLinks: "Social Links",
  home: "Home",
  posts: "Posts",
  "posts.desc": "All the articles I've posted.",
  tag: "Tag",
  "tag.desc": 'All the articles with the tag "{name}".',
  tags: "Tags",
  "tags.desc": "All the tags used in posts",
  archives: "Archives",
  "archives.desc": "All the articles I've archived.",
  featured: "Featured",
  recentPosts: "Recent Posts",
  tableOfContents: "Table of contents",
  "tableOfContents.desc": "Open Table of contents",
  allPosts: "All Posts",
  search: "Search",
  "search.desc": "Search any article",
  "search.placeholder": "Search for anything",
  pages: "Pages",
  page: "Page",
  pageWithNo: "Page {no}",
  "navigation.goBack": "Go back",
  "navigation.goBackHome": "Go Back Home",
  "navigation.backToTop": "Back to Top",
  "navigation.nextPost": "Next Post",
  "navigation.prevPost": "Previous Post",
  pageNotFound: "Page Not Found",
  notFound: "Not Found",
  rssFeed: "RSS Feed",
  suggestChanges: "Suggest Changes",
  toggleLightAndDark: "Toggles light & dark",
  "pagination.prev": "Prev",
  "pagination.next": "Next",
  "footer.text": "Copyright © 2026 Vento | All rights reserved",
  "date.published": "Published",
  "date.updated": "Updated",
  "date.month.1": "January",
  "date.month.2": "February",
  "date.month.3": "March",
  "date.month.4": "April",
  "date.month.5": "May",
  "date.month.6": "June",
  "date.month.7": "July",
  "date.month.8": "August",
  "date.month.9": "September",
  "date.month.10": "October",
  "date.month.11": "November",
  "date.month.12": "December",
  "socials.github": `${baseStrings$2["site.title"]} on Github`,
  "socials.facebook": `${baseStrings$2["site.title"]} on Facebook`,
  "socials.instagram": `${baseStrings$2["site.title"]} on Instagram`,
  "socials.linkedin": `${baseStrings$2["site.title"]} on LinkedIn`,
  "socials.mail": `Send an email to ${baseStrings$2["site.title"]}`,
  "socials.x": `${baseStrings$2["site.title"]} on X`,
  "socials.twitch": `${baseStrings$2["site.title"]} on Twitch`,
  "socials.youtube": `${baseStrings$2["site.title"]} on Youtube`,
  "socials.whatsapp": `${baseStrings$2["site.title"]} on Whatsapp`,
  "socials.snapchat": `${baseStrings$2["site.title"]} on Snapchat`,
  "socials.pinterest": `${baseStrings$2["site.title"]} on Pinterest`,
  "socials.tiktok": `${baseStrings$2["site.title"]} on TikTok`,
  "socials.codepen": `${baseStrings$2["site.title"]} on CodePen`,
  "socials.discord": `${baseStrings$2["site.title"]} on Discord`,
  "socials.gitlab": `${baseStrings$2["site.title"]} on GitLab`,
  "socials.reddit": `${baseStrings$2["site.title"]} on Reddit`,
  "socials.skype": `${baseStrings$2["site.title"]} on Skype`,
  "socials.steam": `${baseStrings$2["site.title"]} on Steam`,
  "socials.telegram": `${baseStrings$2["site.title"]} on Telegram`,
  "socials.mastodon": `${baseStrings$2["site.title"]} on Mastodon`,
  "socials.wechat": `WeChat`,
  "socials.qq": `QQ`,
  "sharePost.desc": "Share this post on",
  "sharePost.on": "Share this post on {media}",
  "sharePost.via": "Share this post via {media}",
  "a11y.skipToContent": "Skip to content",
  "a11y.rssFeed": "rss feed",
  "a11y.openMenu": "Open Menu",
  "a11y.closeMenu": "Close Menu",
  "a11y.archives": "archives",
  "a11y.search": "search",
  "a11y.themeButtonDefaultLabel": "auto",
  "a11y.pagination": "Pagination",
  "a11y.breadcrumb": "breadcrumb",
  "a11y.languagePicker": "Language Picker"
};

const baseStrings$1 = {
  "site.title": "AstroPaper 多语言版",
  "site.desc": "支持多语言的 AstroPaper 主题分支"
};
const CNLocale = {
  ...baseStrings$1,
  "hero.title": "你好",
  "hero.body": `
        AstroPaper 是一个极简、响应式、无障碍且对SEO友好的 Astro 博客主题。
        该主题遵循最佳实践，开箱即提供无障碍支持。
        默认支持浅色和深色模式，此外还可以配置其他配色方案。
`,
  "hero.beforeReadme": "阅读博客文章或查看",
  "hero.readme": "README",
  "hero.afterReadme": "获取更多信息。",
  copy: "复制",
  copied: "已复制",
  by: "作者",
  about: "关于",
  socialLinks: "社交链接",
  home: "首页",
  posts: "文章",
  "posts.desc": "我发布的所有文章。",
  tag: "标签",
  "tag.desc": '所有带有"{name}"标签的文章。',
  tags: "标签",
  "tags.desc": "文章中使用的所有标签",
  archives: "归档",
  "archives.desc": "我归档的所有文章。",
  featured: "精选",
  recentPosts: "最新文章",
  tableOfContents: "目录",
  "tableOfContents.desc": "打开目录",
  allPosts: "所有文章",
  search: "搜索",
  "search.desc": "搜索任意文章",
  "search.placeholder": "搜索内容...",
  pages: "页面",
  page: "页",
  pageWithNo: "第 {no} 页",
  "navigation.goBack": "返回",
  "navigation.goBackHome": "返回首页",
  "navigation.backToTop": "返回顶部",
  "navigation.nextPost": "下一篇",
  "navigation.prevPost": "上一篇",
  pageNotFound: "页面未找到",
  notFound: "未找到",
  rssFeed: "RSS 订阅",
  suggestChanges: "建议修改",
  toggleLightAndDark: "切换浅色/深色模式",
  "pagination.prev": "上一页",
  "pagination.next": "下一页",
  "footer.text": "Copyright © 2026 Vento | All rights reserved",
  "date.published": "发布于",
  "date.updated": "更新于",
  "date.month.1": "一月",
  "date.month.2": "二月",
  "date.month.3": "三月",
  "date.month.4": "四月",
  "date.month.5": "五月",
  "date.month.6": "六月",
  "date.month.7": "七月",
  "date.month.8": "八月",
  "date.month.9": "九月",
  "date.month.10": "十月",
  "date.month.11": "十一月",
  "date.month.12": "十二月",
  "socials.github": `${baseStrings$1["site.title"]} 的 Github`,
  "socials.facebook": `${baseStrings$1["site.title"]} 的 Facebook`,
  "socials.instagram": `${baseStrings$1["site.title"]} 的 Instagram`,
  "socials.linkedin": `${baseStrings$1["site.title"]} 的 LinkedIn`,
  "socials.mail": `发送邮件至 ${baseStrings$1["site.title"]}`,
  "socials.x": `${baseStrings$1["site.title"]} 的 X`,
  "socials.twitch": `${baseStrings$1["site.title"]} 的 Twitch`,
  "socials.youtube": `${baseStrings$1["site.title"]} 的 Youtube`,
  "socials.whatsapp": `${baseStrings$1["site.title"]} 的 Whatsapp`,
  "socials.snapchat": `${baseStrings$1["site.title"]} 的 Snapchat`,
  "socials.pinterest": `${baseStrings$1["site.title"]} 的 Pinterest`,
  "socials.tiktok": `${baseStrings$1["site.title"]} 的 TikTok`,
  "socials.codepen": `${baseStrings$1["site.title"]} 的 CodePen`,
  "socials.discord": `${baseStrings$1["site.title"]} 的 Discord`,
  "socials.gitlab": `${baseStrings$1["site.title"]} 的 GitLab`,
  "socials.reddit": `${baseStrings$1["site.title"]} 的 Reddit`,
  "socials.skype": `${baseStrings$1["site.title"]} 的 Skype`,
  "socials.steam": `${baseStrings$1["site.title"]} 的 Steam`,
  "socials.telegram": `${baseStrings$1["site.title"]} 的 Telegram`,
  "socials.mastodon": `${baseStrings$1["site.title"]} 的 Mastodon`,
  "socials.wechat": `微信`,
  "socials.qq": `QQ`,
  "sharePost.desc": "在以下平台分享此文章",
  "sharePost.on": "在{media}分享此文章",
  "sharePost.via": "通过{media}分享此文章",
  "a11y.skipToContent": "跳至内容",
  "a11y.rssFeed": "RSS订阅",
  "a11y.openMenu": "打开菜单",
  "a11y.closeMenu": "关闭菜单",
  "a11y.archives": "归档",
  "a11y.search": "搜索",
  "a11y.themeButtonDefaultLabel": "自动",
  "a11y.pagination": "分页",
  "a11y.breadcrumb": "面包屑导航",
  "a11y.languagePicker": "语言选择器"
};

const baseStrings = {
  "site.title": "استرو بيبر مترجم",
  "site.desc": "ثيمة متفرعة من استروبيبر مع دعم للترجمة"
};
const ARLocale = {
  ...baseStrings,
  "hero.title": "أهلاً و سهلاً",
  "hero.body": `استروبيبر هو قالب مدونة Astro بسيط، سريع الاستجابة، سهل الاستخدام، ومتوافق مع محركات البحث. يتبع هذا القالب أفضل الممارسات ويوفر إمكانية الوصول للأشخاص ذوى الهمم. يدعم الوضعان الفاتح والداكن افتراضيًا. كما يمكن أيضًا تكوين أنظمة ألوان إضافية.`,
  "hero.beforeReadme": "اقرأ مقالات المدونة أو اطّلع على",
  "hero.readme": "اقرأنى",
  "hero.afterReadme": "لمزيد من المعلومات.",
  copy: "نسخ",
  copied: "تم النسخ",
  by: "بواسطة",
  about: "عنى",
  socialLinks: "تابعنى على",
  home: "الصفحة الرئيسية",
  posts: "المقالات",
  "posts.desc": "كل ما نشرت من مقالات",
  tag: "موضوع",
  "tag.desc": 'كل المقالات المتعلقة بموضوع "{name}".',
  tags: "المواضيع",
  "tags.desc": "كل المواضيع التى كتبت بها",
  archives: "الأرشيف",
  "archives.desc": "جميع المقالات المؤرشفة",
  featured: "المميزة",
  recentPosts: "أحدث المقالات",
  tableOfContents: "جدول المحتويات",
  "tableOfContents.desc": "افتح جدول المحتويات",
  allPosts: "جميع المقالات",
  search: "البحث",
  "search.desc": "ابحث عن أي مقالة تريدها",
  "search.placeholder": "ابحث عن ما تريد",
  pages: "الصفحات",
  page: "الصفحة",
  pageWithNo: "صفحة رقم {no}",
  "navigation.goBack": "اذهب للخلف",
  "navigation.goBackHome": "اذهب للصفحة الرئيسية",
  "navigation.backToTop": "للأعلى",
  "navigation.nextPost": "المقالة التالية",
  "navigation.prevPost": "المقالة السابقة",
  pageNotFound: "هذه الصفحة غير موجودة",
  notFound: "غير موجود",
  rssFeed: "تغذية  بواسطة RSS",
  suggestChanges: "اقترح تعديل",
  toggleLightAndDark: "بدل بين الصبح و الليل",
  "pagination.prev": "السابق",
  "pagination.next": "التالى",
  "footer.text": "حقوق الطبع والنشر © {year} | جميع الحقوق محفوظة",
  "date.published": "نشر فى",
  "date.updated": "تم التحديث فى",
  "date.month.1": "يناير",
  "date.month.2": "فبراير",
  "date.month.3": "مارس",
  "date.month.4": "أبريل",
  "date.month.5": "مايو",
  "date.month.6": "يونيه",
  "date.month.7": "يوليو",
  "date.month.8": "أغسطس",
  "date.month.9": "سبتمبر",
  "date.month.10": "أكتوبر",
  "date.month.11": "نوفمبر",
  "date.month.12": "ديسمبر",
  "socials.github": `${baseStrings["site.title"]} على Github`,
  "socials.facebook": `${baseStrings["site.title"]} على Facebook`,
  "socials.instagram": `${baseStrings["site.title"]} على Instagram`,
  "socials.linkedin": `${baseStrings["site.title"]} على LinkedIn`,
  "socials.mail": `أرسل رسالة إلكترونيى إلى ${baseStrings["site.title"]}`,
  "socials.x": `${baseStrings["site.title"]} على X`,
  "socials.twitch": `${baseStrings["site.title"]} على Twitch`,
  "socials.youtube": `${baseStrings["site.title"]} على Youtube`,
  "socials.whatsapp": `${baseStrings["site.title"]} على Whatsapp`,
  "socials.snapchat": `${baseStrings["site.title"]} على Snapchat`,
  "socials.pinterest": `${baseStrings["site.title"]} على Pinterest`,
  "socials.tiktok": `${baseStrings["site.title"]} على TikTok`,
  "socials.codepen": `${baseStrings["site.title"]} على CodePen`,
  "socials.discord": `${baseStrings["site.title"]} على Discord`,
  "socials.gitlab": `${baseStrings["site.title"]} على GitLab`,
  "socials.reddit": `${baseStrings["site.title"]} على Reddit`,
  "socials.skype": `${baseStrings["site.title"]} على Skype`,
  "socials.steam": `${baseStrings["site.title"]} على Steam`,
  "socials.telegram": `${baseStrings["site.title"]} على Telegram`,
  "socials.mastodon": `${baseStrings["site.title"]} على Mastodon`,
  "sharePost.desc": "شارك هذه المقالة على",
  "sharePost.on": "شارك هذه المقالة على {media}",
  "sharePost.via": "شارك هذه المقالة بواسطة {media}",
  "a11y.skipToContent": "تجاوز إلى المحتوى",
  "a11y.rssFeed": "تغذية  بواسطة rss",
  "a11y.openMenu": "افتح القائمة",
  "a11y.closeMenu": "اغلق القائمة",
  "a11y.archives": "الأرشيف",
  "a11y.search": "البحث",
  "a11y.themeButtonDefaultLabel": "تلقائى",
  "a11y.pagination": "ترقيم الصفحات",
  "a11y.breadcrumb": "اثار الطريق",
  "a11y.languagePicker": "اختر لغة الموقع"
};

const localeToProfile = {
  // locale key must be in lowercase
  en: {
    name: "English",
    messages: ENLocale,
    langTag: "en-US",
    direction: "ltr",
    googleFontName: "IBM+Plex+Mono",
    default: true
  },
  zh: {
    name: "简体中文",
    messages: CNLocale,
    langTag: "zh-CN",
    direction: "ltr",
    googleFontName: "Noto+Sans+SC"
  },
  ar: {
    name: "العربية",
    messages: ARLocale,
    langTag: "ar-EG",
    direction: "rtl",
    googleFontName: "IBM+Plex+Sans+Arabic"
  }
};
const SUPPORTED_LOCALES = Object.keys(localeToProfile);
const DEFAULT_LOCALE = SUPPORTED_LOCALES.find(
  (key) => localeToProfile[key]?.default === true
) ?? SUPPORTED_LOCALES[0];
Object.fromEntries(
  // For Sitemap
  Object.entries(localeToProfile).map(([locale, profile]) => [
    locale,
    profile.langTag
  ])
);

class I18nError extends Error {
  constructor(message) {
    super(message);
    this.name = "I18nError";
  }
}
class UnsupportedLocale extends I18nError {
  constructor(locale) {
    super(`Unsupported locale: ${locale}`);
    this.name = "UnsupportedLocaleError";
  }
}

function appendForwardSlash(path) {
  return path.endsWith("/") ? path : path + "/";
}
function prependForwardSlash(path) {
  return path[0] === "/" ? path : "/" + path;
}
function removeTrailingForwardSlash(path) {
  return path.endsWith("/") ? path.slice(0, path.length - 1) : path;
}
function removeLeadingForwardSlash(path) {
  return path.startsWith("/") ? path.substring(1) : path;
}
function trimSlashes(path) {
  return path.replace(/^\/|\/$/g, "");
}
function isString(path) {
  return typeof path === "string" || path instanceof String;
}
function joinPaths(...paths) {
  return paths.filter(isString).map((path, i) => {
    if (i === 0) {
      return removeTrailingForwardSlash(path);
    } else if (i === paths.length - 1) {
      return removeLeadingForwardSlash(path);
    } else {
      return trimSlashes(path);
    }
  }).join("/");
}
function isRemotePath(src) {
  if (!src) return false;
  const trimmed = src.trim();
  if (!trimmed) return false;
  let decoded = trimmed;
  let previousDecoded = "";
  let maxIterations = 10;
  while (decoded !== previousDecoded && maxIterations > 0) {
    previousDecoded = decoded;
    try {
      decoded = decodeURIComponent(decoded);
    } catch {
      break;
    }
    maxIterations--;
  }
  if (/^[a-zA-Z]:/.test(decoded)) {
    return false;
  }
  if (decoded[0] === "/" && decoded[1] !== "/" && decoded[1] !== "\\") {
    return false;
  }
  if (decoded[0] === "\\") {
    return true;
  }
  if (decoded.startsWith("//")) {
    return true;
  }
  try {
    const url = new URL(decoded, "http://n");
    if (url.username || url.password) {
      return true;
    }
    if (decoded.includes("@") && !url.pathname.includes("@") && !url.search.includes("@")) {
      return true;
    }
    if (url.origin !== "http://n") {
      const protocol = url.protocol.toLowerCase();
      if (protocol === "file:") {
        return false;
      }
      return true;
    }
    if (URL.canParse(decoded)) {
      return true;
    }
    return false;
  } catch {
    return true;
  }
}
function slash(path) {
  return path.replace(/\\/g, "/");
}
function removeBase(path, base) {
  if (path.startsWith(base)) {
    return path.slice(removeTrailingForwardSlash(base).length);
  }
  return path;
}

function shouldAppendForwardSlash(trailingSlash, buildFormat) {
  switch (trailingSlash) {
    case "always":
      return true;
    case "never":
      return false;
    case "ignore": {
      switch (buildFormat) {
        case "directory":
          return true;
        case "preserve":
        case "file":
          return false;
      }
    }
  }
}

function getLocaleRelativeUrl({
  locale,
  base,
  locales: _locales,
  trailingSlash,
  format,
  path,
  prependWith,
  normalizeLocale = true,
  strategy = "pathname-prefix-other-locales",
  defaultLocale
}) {
  const codeToUse = peekCodePathToUse(_locales, locale);
  if (!codeToUse) {
    throw new AstroError({
      ...MissingLocale,
      message: MissingLocale.message(locale)
    });
  }
  const pathsToJoin = [base, prependWith];
  const normalizedLocale = normalizeLocale ? normalizeTheLocale(codeToUse) : codeToUse;
  if (strategy === "pathname-prefix-always" || strategy === "pathname-prefix-always-no-redirect" || strategy === "domains-prefix-always" || strategy === "domains-prefix-always-no-redirect") {
    pathsToJoin.push(normalizedLocale);
  } else if (locale !== defaultLocale) {
    pathsToJoin.push(normalizedLocale);
  }
  pathsToJoin.push(path);
  let relativePath;
  if (shouldAppendForwardSlash(trailingSlash, format)) {
    relativePath = appendForwardSlash(joinPaths(...pathsToJoin));
  } else {
    relativePath = joinPaths(...pathsToJoin);
  }
  if (relativePath === "") {
    return "/";
  }
  return relativePath;
}
function normalizeTheLocale(locale) {
  return locale.replaceAll("_", "-").toLowerCase();
}
function peekCodePathToUse(locales, locale) {
  for (const loopLocale of locales) {
    if (typeof loopLocale === "string") {
      if (loopLocale === locale) {
        return loopLocale;
      }
    } else {
      for (const code of loopLocale.codes) {
        if (code === locale) {
          return loopLocale.path;
        }
      }
    }
  }
  return void 0;
}

function toRoutingStrategy(routing, domains) {
  let strategy;
  const hasDomains = domains ? Object.keys(domains).length > 0 : false;
  if (routing === "manual") {
    strategy = "manual";
  } else {
    if (!hasDomains) {
      {
        strategy = "pathname-prefix-other-locales";
      }
    } else {
      {
        strategy = "domains-prefix-other-locales";
      }
    }
  }
  return strategy;
}

var define_ASTRO_INTERNAL_I18N_CONFIG_default = { format: "directory", trailingSlash: "ignore", i18n: { defaultLocale: "en", locales: ["en", "zh", "ar"], routing: { } }};
const { trailingSlash, format, i18n} = (
  // @ts-expect-error
  define_ASTRO_INTERNAL_I18N_CONFIG_default
);
const { defaultLocale, locales, domains, routing } = i18n;
const base = "/vento/";
let strategy = toRoutingStrategy(routing, domains);
const getRelativeLocaleUrl = (locale, path, options) => getLocaleRelativeUrl({
  locale,
  path,
  base,
  trailingSlash,
  format,
  defaultLocale,
  locales,
  strategy,
  ...options
});

function translateFor(locale, _isLocaleKey = isLocaleKey, _getLocaleMsgs = getLocaleMsgs) {
  if (!_isLocaleKey(locale)) throw new UnsupportedLocale(locale);
  const msgs = _getLocaleMsgs(locale);
  return (key, substitutions) => {
    let translation = msgs[key];
    for (const key2 in substitutions) {
      const value = substitutions[key2];
      translation = translation.replace(`{${key2}}`, String(value));
    }
    return translation;
  };
}
function getLocaleMsgs(locale, getLocaleConfig = getLocaleInfo) {
  return getLocaleConfig(locale).messages;
}
function isLocaleKey(locale, supportedLocales = SUPPORTED_LOCALES) {
  if (typeof locale !== "string") return false;
  return supportedLocales.includes(locale);
}
function getLocaleInfo(locale, _isLocaleKey = isLocaleKey) {
  if (!_isLocaleKey(locale)) throw new UnsupportedLocale(locale);
  return localeToProfile[locale];
}
function getRelativeLocalePath(locale, path = "/", {
  _isLocaleKey = isLocaleKey,
  ...options
} = {}) {
  if (!_isLocaleKey(locale)) throw new UnsupportedLocale(locale);
  const localizedPath = getRelativeLocaleUrl(locale, path, options);
  const hasTrailingSlash = path.endsWith("/") || localizedPath === "/";
  if (hasTrailingSlash) return localizedPath;
  return localizedPath.replace(/\/+$/, "");
}
function stripBaseAndLocale(locale, path, _isLocaleKey = isLocaleKey, _buildPrefix = buildPrefix) {
  if (!_isLocaleKey(locale)) throw new UnsupportedLocale(locale);
  const prefix = _buildPrefix(locale);
  return new URL(path.slice(prefix.length), "http://foo.com").pathname;
}
function buildPrefix(locale, defaultLocale = DEFAULT_LOCALE, baseUrl = "/vento/") {
  const isDefaultLocale = locale === defaultLocale;
  const baseWithLocale = baseUrl.replace(/\/+$/, "") + (isDefaultLocale ? "" : `/${locale}`);
  return new URL(baseWithLocale, "http://foo.com").pathname;
}
function getSlugWithoutLocale(slug) {
  const slugParts = slug.split("/");
  if (slugParts.length > 1 && SUPPORTED_LOCALES.includes(slugParts[0])) {
    return slugParts.slice(1).join("/");
  }
  return slug;
}

const SITE = {
  website: "https://zhangxiaohh.github.io/vento/",
  // GitHub Pages 部署地址
  author: "Yousef Elsayed",
  profile: "https://yousef8.github.io/astro-paper-i18n",
  ogImage: "astropaper-og.jpg",
  postPerIndex: 4,
  postPerPage: 4,
  scheduledPostMargin: 15 * 60 * 1e3,
  // show back button in post detail
  editPost: {
    url: "https://github.com/yousef8/astro-paper-i18n/edit/main/"
  },
  dynamicOgImage: true,
  // html lang code. Set this empty and default will be "en"
  timezone: "Asia/Bangkok"
  // Default global timezone (IANA format) https://en.wikipedia.org/wiki/List_of_tz_database_time_zones
};

export { DEFAULT_LOCALE as D, SUPPORTED_LOCALES as S, SITE as a, getRelativeLocalePath as b, getSlugWithoutLocale as c, slash as d, isRemotePath as e, getLocaleInfo as g, isLocaleKey as i, joinPaths as j, prependForwardSlash as p, removeBase as r, stripBaseAndLocale as s, translateFor as t };
